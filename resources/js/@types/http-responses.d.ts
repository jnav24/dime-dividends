declare namespace HttpResponses {
    enum HttpError {
        DEFAULT = 'Something unexpected had occurred.',
        PASSWORD_CONFIRM = 'Password confirmation required.'
    }
}

export = HttpResponses;
