import React from 'react';

// @ts-ignore
import Guest from '@/Pages/layouts/Guest';

const Login = () => {
    return (
        <Guest>
            <h1>Login</h1>
        </Guest>
    );
};

export default Login;
